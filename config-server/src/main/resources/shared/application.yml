
#This configuration file will hold common configs to be shared among all files
eureka:
  instance:
    prefer-ip-address: false
  client:
    serviceUrl:
      defaultZone: http://registry-service:8761/eureka #where our registry server is located

security:
  oauth2:
    resource:
      user-info-uri: http://auth-service:8044/uaa/users/current #which uri will be used to find the useru

#hystrix:
#  command:
#    default:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 20000