
spring:
  profiles:
    active: dev
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      username: user
      password: password
